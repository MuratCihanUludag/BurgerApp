

<section class="section-1">
    <div class="section-admin-img"></div>
    <div class="table-responsive-container container">
        <div class="table-responsive"> </div>
    </div>
</section>

<!-- Edit-Modal -->

<div class="modal fade" id="btn-edit-cips" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Burger Duzenle</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div id="cips-edit"></div>
        </div>
    </div>
</div>

<!-- Delete-Modal -->

<div class="modal fade" id="btn-delete-cips" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Burger Sil</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div id="burger-delete"> </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(() => {
            $.ajax({
                url: `/admin/cips/getTableList`,
                method: 'GET',
                beforeSend: () => {
                    console.log("bekleyin")
                },
                success: (data) => {
                    $(".table-responsive").html(data);
                    BindButton()
                }
            })
        })
        function BindButton() {
            $(".btn-edit-cips").click((e) => {
                var id = e.target.id;
                $.ajax({
                    url: `/admin/cips/edit/${id}`,
                    method: "GET",
                    success: (data) => {
                        $("#cips-edit").html(data);
                    },
                    error: (xhr) => {
                        console.log(xhr.status)
                    }
                })
            })

            $(".btn-delete-cips").click((e) => {
                var id = e.target.id;
                $.ajax({
                    url: `/admin/cips/delete/${id}`,
                    method: "GET",
                    success: (data) => {
                        $("#burger-delete").html(data);
                    },
                    error: (xhr) => {
                        console.log(xhr.status)
                    }
                })
            })
        }



    </script>
}